# Dual-Brain: SO-101 자율실험실 프로젝트

## 실험 브랜치 현황 (2026-08-18)

이 브랜치는 Mac에서 전달받은 ACT v2.2를 기준으로 **0.4초 재관측**, **목표 상공 6cm 놓기**, **박스 바닥면 75% 이상 포함 성공 판정**, **실패 후 재집기·전체 작업 복구**를 실험한 코드입니다. 아직 `main`에 병합하지 않은 검토용 브랜치입니다.

| 항목 | 현재 결론 |
|---|---|
| 현행 정책 후보 | Mac ACT v2.2 + `n_action_steps=10` |
| 기본 성공률 | 고정 시드 50회 중 24회, 48% |
| 좌표 기반 전체 복구 | 고정 시드 20회 중 18회, 90% — MuJoCo 전용 성능 상한 실험 |
| 손목 영상 집기 감시 | 최신 상단 카메라 결합 검증에서 10회 중 9회 일치 |
| 상단 영상 75% 성공 판정 | 최신 10회에서 10회 모두 일치 |
| 상단 영상 낙하 판정 | 오탐은 없었지만 실제 낙하 1건 미탐 — 능동 복구 연결 보류 |

이번 브랜치에는 6cm 교사 동작, DAgger·재집기 데이터 생성기, 좌표 기반 감독, 손목/상단 영상 그림자 감독, 평가·벤치마크 도구와 MuJoCo 상단 카메라 설정이 포함됩니다. 재현에 필요한 현행 모델과 핵심 데이터셋은 `artifacts/`에 선별해 포함하고, 평가 영상 모음·`results/` 원시 결과·별도 종합보고서는 포함하지 않습니다.

### 날짜별 작업 기록

| 날짜 | 진행 내용 | 결과·결정 |
|---|---|---|
| 2026-08-11 | 기현 → 도영 인수, 원본 `lerobot-kwonlab` 분석 및 핵심 코드를 `dual-brain`에 통합. Windows·LeRobot 0.6.0·MuJoCo 호환성 확인. 성공 기준을 블록 바닥면 75% 이상 포함으로 통일. 놓기 높이 8·6·5·4cm 비교 및 교사 궤적 개선. | 6cm를 기본 높이로 채택. 개선 교사는 고정 시드 50회에서 43회 성공(86%), 성공 43건은 목표 포함률 100%. 12cm 경로는 롤백용으로 격리. |
| 2026-08-12 | Mac ACT v2.2를 0.4초 재관측으로 평가. 6cm 성공 데이터 200에피소드(v2.4) 생성 및 파인튜닝. 집기·운반·놓기 실패를 나눈 DAgger 50에피소드 수집, 일반 데이터와 혼합·정규화·비율 대조 실험. | 현행 v2.2는 24/50(48%). v2.4 3,000스텝은 18/50(36%)로 미채택. DAgger 비중 22.7%의 최고 결과도 5/20(25%)로 기준선보다 낮아 추가 학습 중단. |
| 2026-08-13 | 집기 실패 후 빈 운반을 막는 재집기 감독, 운반 낙하·잘못 놓기·시간초과 전체 복구 구현. 손목 RGB와 관절만 사용하는 그림자 감독을 분리해 검증. | 난수 고정 기준 v2.2 10/20(50%) → MuJoCo 좌표 기반 전체 복구 18/20(90%). 손목 영상 집기 판정은 20/20 일치했지만 낙하·놓기 판정은 부족해 제어 연결 보류. |
| 2026-08-14 | 새 실험·코드 변경 기록 없음. | 8월 13일 검증 결과 유지. |
| 2026-08-15 | 새 실험·코드 변경 기록 없음. | 8월 13일 검증 결과 유지. |
| 2026-08-16 | 새 실험·코드 변경 기록 없음. | 8월 13일 검증 결과 유지. |
| 2026-08-17 | 새 실험·코드 변경 기록 없음. | 8월 13일 검증 결과 유지. |
| 2026-08-18 | 52cm 높이 U20CAM-720P 상단 카메라를 MuJoCo에 추가. 좌표 없이 목표 마스크와 블록 영상 면적을 이용하는 75% 성공 판정 및 낙하 그림자 감시 구현. 별도 검토 브랜치와 Draft PR 생성. | 최신 10회에서 놓기 성공 판정 10/10, 집기 판정 9/10 일치. 낙하 오탐은 제거했지만 실제 낙하 1건을 놓쳐 자동 재집기 연결은 계속 보류. |

### 이 브랜치에 포함되는 재현 자산

Git LFS를 설치한 뒤 `git lfs pull`을 실행하면 다음 자산을 받을 수 있습니다.

| 경로 | 용도 |
|---|---|
| `artifacts/checkpoints/act_pick_v22/pretrained_model/` | 현재 기준선 ACT v2.2 추론·평가·파인튜닝 |
| `artifacts/datasets/so101_sim_pick_v22/` | Mac v2.2 원본 손목 카메라 200에피소드 데이터 |
| `artifacts/datasets/so101_sim_pick_v24/` | 개선된 6cm 교사 성공 데이터 200에피소드 |
| `artifacts/datasets/so101_sim_pick_dagger_v2_balanced50/` | 집기 20·운반 15·놓기 15의 균형 교정 데이터 |

중간 학습 체크포인트 전체, 폐기된 실험 데이터, 평가 영상 모음, `outputs/`, `results/`, 별도 종합보고서는 포함하지 않습니다. 이들은 재현에 필수적이지 않거나 용량이 지나치게 크며, 보고서는 이전 요청에 따라 GitHub 업로드 대상에서 제외합니다.

## 2026-08-13 집기 확인·전체 작업 복구 실험

> 작업자: 도영. 아래 코드는 실험 브랜치에 게시하며, 대용량 학습·평가 산출물은 로컬에 보관합니다.

- 원본 Mac v2.2는 집기에 실패해도 빈 그리퍼로 목표 구역까지 이동하는 문제가 있었습니다. `kwon_lab/skills/supervision.py`와 `eval_act_pick.py --retry-pick --full-recovery`에 MuJoCo 상태 기반 감독을 추가했습니다.
- 단순 `5초 안에 들리지 않음` 판정은 폐기했습니다. 정상 동작도 최초 들림이 약 9초까지 늦어질 수 있어 조기 개입이 정상 궤적을 실패로 오인했기 때문입니다. 현재는 블록이 바닥에 남은 채 그리퍼가 블록에서 16cm 이상 떨어져 목표 XY 14cm 안으로 이동한 경우를 빈 운반으로 판정하고, 애매한 경우에는 240스텝을 최종 기한으로 씁니다.
- 미집기 시 ACT 큐를 비우고 `그리퍼 개방 → 20cm 안전 이탈 → 손목 카메라 재조준 → 재집기`를 최대 3회 수행합니다. 집은 뒤 운반 중 떨어뜨리거나 75% 포함 성공 기준을 만족하지 못하면 최대 2회 다시 집어 목표 상공 6cm에서 놓습니다.
- 이 복구 교사는 **MuJoCo의 블록·목표 좌표를 사용하는 시뮬레이션 전용 상한선 실험**입니다. v2.2 자체가 6cm 동작으로 재학습된 것은 아니며, 감독이 개입한 복구 구간에서만 6cm 교사가 동작합니다.

난수를 고정한 시드 5000~5019, 0.4초 재관측, 블록 면적 75% 이상 포함 기준 결과:

| 실행 | 성공률 | 평균 최종 거리 |
|---|---:|---:|
| 원본 v2.2 | 10/20 (50%) | 94.1mm |
| v2.2 + 좌표 기반 전체 복구 | **18/20 (90%)** | **54.5mm** |

- 10편에서 집기 재시도가 발동해 총 15회 재집기했고, 12편에서 총 16회의 낙하·놓기·최종 복구가 발동했습니다. 실패는 시드 5006(블록이 약 435mm 밖으로 밀려 재집기 불가)과 5009(운반 중 반복 낙하, 최종 약 354mm 이탈) 2편입니다.
- 이전 탐색 결과 14/20(집기 재시도만)과 19/20(전체 복구)은 정책 VAE 난수를 명시적으로 고정하기 전 측정이므로 최종 수치로 사용하지 않습니다. 최종 결과는 `outputs/eval/act_pick_v22/act_pick_v22_seeded_baseline_eval20`과 `act_pick_v22_seeded_full_recovery_eval20`입니다.
- `kwon_lab/skills/vision_supervision.py`에는 MuJoCo 좌표를 받지 않고 손목 RGB와 관절 상태만 사용하는 그림자 감독을 분리했습니다. 집기 판단은 별도 20회에서 미집기 8·정상 집기 12를 오검출/누락 없이 구분했고, 전체 작업 20회에서도 집기 10·미집기 10을 모두 구분했습니다.
- 반면 전체 작업 그림자 검증에서 운반 낙하는 4건 중 0건, 성공 놓기는 6건 중 2건만 검출했습니다. 따라서 카메라 감독은 아직 제어에 연결하지 않고 그림자 모드로 유지합니다. 결과는 `outputs/eval/vision_supervision/*_seeded_eval20.json`에 있습니다.
- `generate_dagger_dataset.py`는 같은 상태 기반 판정을 쓰도록 고쳤고, `generate_retry_pick_dataset.py`에는 카메라가 미집기를 감지한 상태에서 짧은 재집기 교정 데이터만 만드는 경로를 마련했습니다. 기존 5초 조기 개입 DAgger 데이터는 다음 학습에 그대로 재사용하지 않으며, 새 데이터 수집·학습은 카메라 낙하/놓기 감독을 보강한 뒤 별도 승인 후 진행합니다.

## 2026-08-12 로컬 실험 현황 — DAgger 50개 1차 파인튜닝

### v2.2 정규화 고정 대조 실험

- LeRobot 파인튜닝 경로는 사전학습 모델의 전처리기를 읽더라도 새 데이터셋의 통계로 normalizer를 덮어씁니다. 이를 분리하기 위해 원본 혼합 데이터의 Parquet·영상을 그대로 참조하고 `meta/stats.json`만 Mac v2.2 데이터 통계로 교체한 로컬 전용 뷰 `C:\SO101_data\so101_sim_pick_dagger_mix_v1_v22stats`를 만들었습니다.
- 3스텝 점검과 500스텝 체크포인트 모두에서 저장된 action·state 정규화 20개 항목이 v2.2 원본과 완전히 일치함을 확인했습니다.
- 나머지 조건을 같게 유지한 500스텝 파인튜닝 후 동일 시드 20회 평가 결과는 **1/20(5%)**, 평균 최종 거리 207.4mm, 최대 포함률 100%였습니다. 새 혼합 통계를 사용한 500스텝 결과 0/20보다 1건 늘었지만 현행 48%와 큰 차이가 있어 정규화 변경만으로는 성능 저하를 설명할 수 없습니다.
- 다음 대조 실험은 v2.2 정규화를 계속 고정하면서 일반 6cm 데이터를 200편으로 늘리고 DAgger 교정 프레임 비율을 약 20~30%로 낮춰 기존 행동 망각 여부를 확인합니다.

### DAgger 비중 22.7% 대조 실험

- 영상 재인코딩 없이 검증된 150편 혼합 데이터에서 일반 6cm 에피소드 100편 전부와 DAgger 15편만 학습 로더로 선택했습니다. DAgger는 원본 50편에서 집기 6·운반 5·놓기 4편을 균형 선택했으며, 일반 24,400프레임 + 교정 7,168프레임으로 교정 비중은 22.71%입니다.
- 정규화는 계속 Mac v2.2 통계로 고정하고 학습률 `5e-7`, 배치 2, `n_action_steps=10`으로 학습했습니다.

| 체크포인트 | 성공률 | 평균 최종 거리 | 평균 구역 포함률 |
|---|---:|---:|---:|
| 500 | **5/20 (25%)** | **130.5mm** | **31.2%** |
| 1,000 | 2/20 (10%) | 181.9mm | 12.4% |

- DAgger 51.5%·v2.2 정규화·500스텝의 1/20(5%)보다 22.7% 혼합의 500스텝이 5/20(25%)로 명확히 회복했습니다. 따라서 교정 데이터 과대표현과 기존 행동 망각이 주된 성능 저하 원인이라는 근거가 생겼습니다.
- 정확히 같은 시드 5000~5019에서 현행 v2.2는 10/20(50%), 평균 최종 거리 102.4mm입니다. DAgger 22.7%·500스텝은 5/20(25%), 130.5mm이므로 동일 조건에서도 열세입니다. 성공 시드도 현행과 겹치는 것은 5007 하나뿐이고, 기존 실패 시드 5003·5004·5006·5015를 새로 성공한 대신 기존 성공 9개를 잃었습니다. 즉 복구 능력 일부를 얻었지만 정상 행동 보존에 실패했습니다.
- 1,000스텝에서는 다시 10%로 하락했으므로 추가 학습은 중단합니다. 현재 DAgger 실험 중 최선은 `C:\SO101_train\act_pick_dagger22pct_v22stats_500\checkpoints\000500`이지만 현행 Mac v2.2의 24/50(48%)보다 낮아 운용 모델로 채택하지 않습니다.
- 다음 권장안은 전체 복구 에피소드를 더 섞는 것이 아니라, DAgger 에피소드에서 개입 직전 약 0.4~0.8초와 교사 복구 초반만 남긴 짧은 교정 구간을 만들고 비중을 10~15%로 제한하는 것입니다. 새 모델은 250·500스텝을 각각 20회 평가하고, 현행 48%를 넘거나 최소한 같은 20개 시드에서 기준선보다 좋아질 때만 50회 정식 평가합니다.

- 6cm 일반 성공 데이터 100에피소드와 균형 DAgger 교정 데이터 50에피소드를 합쳐 `C:\SO101_data\so101_sim_pick_dagger_mix_v1`을 만들었습니다. 총 150에피소드·50,324프레임이며, 프레임 비율은 일반 48.49%·DAgger 51.51%입니다. 12cm 데이터는 포함하지 않았습니다.
- 혼합 데이터의 Parquet·영상 디코딩·LeRobot 표본 로딩을 모두 검증했습니다. 생성 스크립트는 `kwon_lab/datagen/build_dagger_training_mix.py`, 구성 기록은 데이터셋 루트의 `mix_manifest.json`에 있습니다.
- Mac v2.2 가중치에서 학습률 `5e-7`, 배치 2, `n_action_steps=10`, CUDA AMP로 1,500스텝 파인튜닝했습니다. 체크포인트는 500스텝마다 `C:\SO101_train\act_pick_dagger_mix_v1\checkpoints`에 저장했습니다. GitHub에는 업로드하지 않았습니다.
- 동일한 시드 5000~5019와 0.4초 재관측, 박스 면적 75% 이상 포함 성공 기준으로 각 체크포인트를 20회 평가했습니다.

| 체크포인트 | 성공률 | 평균 최종 거리 | 최대 구역 포함률 |
|---|---:|---:|---:|
| 500 | 0/20 (0%) | 207.4mm | 0% |
| 1,000 | 0/20 (0%) | 201.2mm | 65.6% |
| 1,500 | 2/20 (10%) | 185.4mm | 100% |

- 학습량이 늘면서 거리와 최대 포함률은 개선됐지만, 1,500스텝도 현행 Mac v2.2 기준선 24/50(48%)보다 크게 낮습니다. 따라서 이 파인튜닝 모델은 채택하지 않으며 현행 모델은 계속 **Mac v2.2 + 0.4초 재관측**입니다.
- 가장 강한 원인 후보는 초기 가중치가 학습된 v2.2 정규화 통계와 새 혼합 데이터 정규화 통계의 차이입니다. 예를 들어 action 평균은 v2.2의 `[-0.330, -0.765, 0.713, 0.925, 0.346, 0.691]`에서 혼합 데이터의 `[-0.424, -0.617, 0.512, 1.096, 0.222, 0.902]`로 바뀌었고, DAgger 복구 자세가 기존 범위를 크게 벗어난 축도 있습니다. 기존 가중치의 출력 해석이 처음부터 달라진 상태에서 적은 스텝으로 재적응한 것이므로, 정규화 호환성 실패 여부를 다음 대조 실험에서 분리해야 합니다.
- 다음 권장 실험은 데이터를 더 수집하는 것이 아니라, 같은 혼합 데이터와 v2.2 정규화 통계를 유지한 채 500스텝만 파인튜닝하고 20회 평가하는 것입니다. 이 대조군이 회복하면 정규화가 원인이며, 회복하지 않으면 DAgger 비율을 프레임 기준 20~30%로 낮추고 개입 이후 짧은 복구 구간만 쓰는 방향으로 데이터 혼합을 조정합니다.

## 2026-08-12 로컬 실험 현황 — v2.4 성공 시연 보강

> 작업자: 도영. v2.4 데이터셋은 재현 자산으로 포함하며, 중간 학습 체크포인트와 평가 원시 결과는 로컬에 보관합니다.

- 현행 실행 기준선은 Mac에서 전달받은 `act_pick_v22/pretrained_model`이며, 0.4초마다 재관측하도록 `n_action_steps=10`을 적용했을 때 현재 성공 기준(박스 면적의 3/4 이상이 목표 구역 안)에 대해 **24/50, 48%**였습니다.
- 개선된 집기·운반과 목표 상공 6cm 놓기 교사를 사용해 `so101_sim_pick_v24` 성공 에피소드 200개를 새로 생성했습니다. 층화 초기 자세 200칸을 모두 채웠고, 211회 시도 중 200회 성공(수집 성공률 95%), 총 48,800프레임입니다.
- Mac v2.2 가중치에서 v2.4 데이터로 저학습률 파인튜닝했습니다. 1,000·2,000·3,000·4,000스텝 체크포인트를 저장했으며, 10회 선별 평가에서는 각각 50%·50%·60%·10%였습니다. 4,000스텝의 급락 때문에 5,000스텝까지 계속하지 않았습니다.
- 선별 최고였던 3,000스텝을 50회 정식 평가한 결과는 **18/50, 36%**였습니다. 성공한 경우의 평균 중심 오차는 18.2mm, 평균 구역 포함률은 99.9%로 v2.2의 성공 샘플(20.3mm, 98.5%)보다 놓기 정밀도는 좋아졌지만, 멀리 벗어난 실패가 20회에서 30회로 늘어 전체 성공률은 낮아졌습니다.
- 따라서 v2.4 파인튜닝 모델은 현행 모델로 채택하지 않습니다. 현재 운용 후보는 계속 **Mac v2.2 + 0.4초 재관측**입니다.
- 이번 결과는 성공 시연만 추가해도 학생이 실제 롤아웃에서 만나는 집기 실패·이탈 상태의 복구법은 배우지 못하며, 성공 궤적 분포에 과적합하거나 기존 능력을 잊을 수 있음을 보여줍니다. 다음 우선순위는 성공 에피소드 추가가 아니라 **DAgger 교정 데이터 수집**입니다.

다음 실험은 v2.2 학생을 0.4초 재관측으로 실행하고, 집기 실패·운반 이탈·놓기 직전 오차가 처음 발생한 학생 상태에서 6cm 교사가 복구 행동을 라벨링하는 방식으로 진행합니다. 교정 데이터 약 100에피소드부터 시작하고, 학습률 `5e-7`, 500스텝 간격 평가, 중간 평가는 최소 20회, 최종 평가는 고정 시드 50회를 적용하는 것을 권장합니다.

### DAgger 수집기 10에피소드 검증

- `generate_dagger_dataset.py`가 Mac v2.2 학생을 0.4초 재관측으로 실행하고, 5초 안에 들지 못함·운반 중 낙하·8초 안에 놓지 못함을 자동 감지합니다.
- 학생이 이미 성공한 에피소드와 실패한 학생 행동은 저장하지 않습니다. 실패가 감지된 현재 상태에서 6cm 교사가 인계한 이후의 교정 관측·행동만 저장합니다.
- 시드 8000~8019의 20회 시도에서 학생 자체 성공 3회와 교사 복구 실패 7회를 제외하고, 교사 복구에 성공한 교정 에피소드 10개를 저장했습니다.
- 저장된 유형은 5초 내 미집기 복구 9개, 운반 중 낙하 복구 1개이며, 모두 재집기 후 6cm 놓기입니다. 총 2,440프레임이고 각 에피소드는 244프레임입니다.
- 최종 평균 중심 오차는 7.51mm, 평균 구역 포함률은 100%, 최대 최종 블록 중심 높이는 2.994cm였습니다.
- Parquet 2,440행, 영상 디코딩 2,440프레임, 에피소드 인덱스 10개, 상태·행동 shape `(2440, 6)`, NaN/Inf 없음, LeRobot+PyAV 표본 로딩을 모두 확인했습니다.
- 기존 12cm 데이터·모델·평가는 활성 학습 경로에서 제외하고 `C:\SO101_legacy_12cm\mac-artifacts`에 롤백용으로 격리했습니다. DAgger는 그 안의 v2.2 가중치만 초기 학생으로 읽으며 12cm 데이터는 섞지 않습니다.

10개 시험 데이터는 구조적으로 정상입니다. 다만 10개 중 9개가 미집기 복구에 집중됐으므로 곧바로 100개를 같은 비율로 늘리기보다, 다음 수집기에서 집기·운반·놓기 실패 유형별 목표 개수를 설정하고 교사가 복구하지 못한 상태의 원인을 먼저 보강하는 것이 권장됩니다.

### DAgger 복구·균형 수집 개선 검증

- 교사는 학생 실패 자세에서 바로 재집기하지 않고 `그리퍼 개방 → 현재 XY에서 20cm 안전 이탈 → 손목캠 재조준 → 재집기 → 6cm 놓기` 순서로 복구합니다.
- 개입 순간의 블록·목표·그리퍼 위치, 관절 상태·명령, 목표 거리·포함률과 안전 이탈 IK 오차, 재조준·재집기·놓기의 단계별 결과를 manifest에 저장합니다.
- 블록이 테이블로 내려온 실패를 목표에서 8cm보다 멀면 운반 낙하, 8cm 이내면 놓기 오차로 분리했습니다. 집기·운반·놓기 유형별 저장 정원을 지정할 수 있으며, 이미 찬 유형은 저장하지 않습니다.
- 강화 복구 스모크 3개는 3회 시도 모두 복구에 성공했습니다. 총 1,844프레임, 최종 평균 중심 오차 4.55mm였고 데이터·영상 검증을 통과했습니다.
- 균형 시험은 8회 시도에서 집기 미성공 1개, 운반 중 낙하 1개, 잘못 놓음 1개를 각각 저장했습니다. 학생 자체 성공 2개와 정원이 찬 집기 실패 3개는 저장하지 않았고, 교사 복구 실패는 0개였습니다.
- 균형 데이터는 총 1,468프레임이며 에피소드별 436·700·332프레임입니다. Parquet·영상 프레임 수, 에피소드 인덱스, 유한한 상태·행동 값, LeRobot+PyAV 디코딩과 최종 포함률 100%를 확인했습니다.

다음 본 수집은 100개를 한 번에 고정하기보다 1차로 집기 20·운반 15·놓기 15의 균형 교정 50개를 수집해 파인튜닝·20회 평가하는 것을 권장합니다. 기준선 48%에 개선 신호가 있으면 같은 비율로 50개를 추가하고, 없으면 100개 전체를 만들기 전에 개입 기준과 데이터 혼합을 조정합니다.

### DAgger 균형 교정 50개 본 수집

- 시드 8200부터 실제 v2.2 학생을 0.4초 재관측으로 실행해 총 984회 탐색했고, 집기 20·운반 15·놓기 15의 정원을 정확히 채웠습니다. 인위적으로 실패를 만들지 않고 실제 학생 롤아웃에서 발생한 상태만 사용했습니다.
- 세부 구성은 5초 내 미집기 20개, 운반 중 낙하 15개, 목표 근처 잘못 놓음 12개, 8초 내 미놓기 3개입니다. 교정 방식은 안전 이탈·재조준·재집기·6cm 놓기 47개와 들고 있는 블록을 바로 6cm로 놓는 교정 3개입니다.
- 학생 자체 성공 159개와 교사 복구 실패 14개는 저장하지 않았습니다. 교사 실패 단계는 재집기 7개, 재조준 6개, 6cm 놓기 1개였습니다.
- 저장 데이터는 50에피소드, 25,924프레임, 약 93.75MB입니다. 에피소드 길이는 최소 108·최대 992·평균 518.48프레임입니다.
- Parquet 25,924행, 영상 메타데이터와 실제 디코딩 25,924프레임, 에피소드 인덱스 0~49, 상태·행동 유한값, LeRobot+PyAV 표본 로딩을 확인했습니다.
- 모든 교정 에피소드의 최종 구역 포함률은 100%였습니다. 평균 중심 오차는 8.02mm, 최대 중심 오차는 17.49mm, 최대 최종 블록 중심 높이는 2.994cm입니다.
- 다음 단계는 이 50개 교정 데이터로 Mac v2.2에서 학습률 `5e-7` 파인튜닝을 수행하고, 500스텝 간격 20회 롤아웃으로 현재 기준선 48%와 비교하는 것입니다.

> 현재 작업자: 도영 (2026-08-11 인수)

SO-101 로봇팔에 느린 전략 계층인 **System 2**와 빠른 행동 계층인 **System 1**을 결합하여, 자연어 명령을 실험 계획·로봇 조작·관찰·검증·보고까지 연결하는 프로젝트입니다.

최종 데모는 사용자가 “이 용액의 pH를 알아내 봐”라고 말하면 로봇이 필요한 실험을 계획하고, 시약과 도구를 조작하고, 결과를 관찰한 뒤 실험 로그와 보고서를 만드는 자율실험실입니다. 장기적으로 축적하려는 핵심 기술 자산은 특정 로봇이나 단일 정책 가중치가 아니라, 어떤 기계 몸에도 연결할 수 있는 **embodiment-independent orchestration software**입니다.

> 2026-08-11 인수인계 문서와 프로젝트 원장을 반영했습니다. 아래의 기존 렌즈 뚜껑 실물 프로젝트 기록은 Dual-Brain으로 확장되기 전의 실제 로봇 기준선으로 보존합니다. 실제 로봇을 움직이기 전에는 항상 주변을 비우고, 비정상 진동이나 충돌이 발생하면 팔로워의 12V 전원을 즉시 분리합니다.

## 프로젝트의 핵심 가설

일반적인 로봇 학습은 사람이 장시간 시연 데이터를 만들어야 합니다. 이 프로젝트는 그 선생을 사람에서 System 2로 교체합니다.

1. System 2가 새로운 명령을 이해하고 성공 조건과 행동 레시피를 설계합니다.
2. 시뮬레이션에서 레시피 또는 탐색기가 여러 번 시도합니다.
3. 외부 성공 판정기가 결과를 채점합니다.
4. 검증된 궤적을 LeRobot 형식의 학습 데이터로 저장합니다.
5. ACT 등의 System 1 정책이 카메라와 관절 상태만 보고 행동하도록 학습합니다.
6. 배포 시 반복 작업은 System 1이 빠르게 수행하고, 실패하거나 낯선 상황에서는 System 2가 개입합니다.

핵심 명제는 다음과 같습니다.

> 사람 시연 시간을 늘리는 대신, LLM이 실험을 설계하고 시뮬레이션이 데이터를 생산하며 성공 판정기가 품질을 관리한다.

## 이중 시스템 아키텍처

| 계층 | 실행 주기 | 역할 |
|---|---:|---|
| System 2 | 이벤트 단위, 약 0.2~0.3Hz | 자연어 명령 해석, 태스크 분해, 전략 수립, 성공 조건 작성, 스킬 선택, 실패 복구, 보고서 작성 |
| System 1 | 약 10~100Hz | ACT 같은 학습 정책과 IK·모션 플래닝 같은 고전 제어 스킬을 실행 |
| 서보·안전 | 100Hz 이상 | 관절 목표 추종, 토크 제한, 작업 공간 및 비상 정지 관리 |

System 2는 고주파수 제어 루프에서 매 동작을 직접 결정하지 않습니다. 파라미터 탐색은 CEM·랜덤 탐색 등의 수치 최적화기가 맡고, System 2는 탐색 범위·목표·검증기를 설계합니다. System 1의 각 스킬은 장기적으로 다음 정보를 포함하는 레지스트리 항목으로 관리합니다.

```text
skill = {
  policy_or_controller,
  preconditions,
  effects,
  success_verifier,
  timeout,
  recovery_strategy
}
```

## 에고센트릭 설계 원칙

2026-08-10부터 런타임의 세계 좌표 인터페이스를 폐기하고 손목 카메라 중심의 에고센트릭 구조로 전환했습니다.

- 모델 입력은 몸에 부착된 손목 카메라와 관절 상태를 사용합니다.
- 고정 `front` 카메라는 사람의 관전과 영상 기록에만 사용하며 모델 입력에는 넣지 않습니다.
- 물체의 세계 좌표와 시뮬레이션 정답 상태는 외부 채점 및 선생 데이터 라벨링에만 사용합니다.
- 런타임 제어는 카메라 기준 상대 이동, 관절 직접 제어, 로컬 시각 조준 스킬로 수행합니다.
- 손목 카메라는 FK와 관절 엔코더로 중력 방향을 계산해 영상을 자동 정립합니다. 실물에서는 캘리브레이션된 기구학 모델로 같은 인터페이스를 유지합니다.
- 새 기능을 추가할 때도 채점·진단 목적 외에는 특권 좌표를 정책이나 LLM에 노출하지 않습니다.

이 결정의 배경은 고정 카메라 정책이 바닥과 조명을 바꾸자 성공률이 50%에서 15%로 떨어진 실험입니다. 몸에 붙은 카메라를 사용하면 외부 카메라 배치에 대한 구조적 의존을 줄일 수 있습니다.

## 현재까지 검증한 가설

| 가설 | 실험 | 1차 결과 |
|---|---|---|
| LLM이 사람 선생을 대체할 수 있다 | 레시피 선생이 생성한 시뮬레이션 데이터로 ACT 학습 | 초기 실험에서 학생 정책이 선생의 50% 성공률에 도달 |
| 특권 좌표 없이 손목 카메라로 조작할 수 있다 | 로컬 Gemma가 스캔→인식→센터링→레이캐스트→그랩 수행 | 손목캠 단독, 약 1.5cm 오차로 첫 그랩 성공, API 비용 $0 |
| 사전 지식 없이 몸을 발견할 수 있다 | 이름 없는 모터 6개와 카메라만 제공하는 `unknown body` 실험 | Claude가 17회 호출·약 2.6분에 인과지도를 만들고 블록 접촉 성공 |

로컬 VLM은 한 번에 정확한 픽셀을 지목하는 방식에서는 실패했지만, 거친 격자 판단을 반복하는 폐루프에서는 성공했습니다. 따라서 지각 모델의 원샷 정밀도보다 **관찰→조정→재관찰 루프의 설계**가 더 중요한 것으로 판단합니다.

## 에고센트릭 ACT 실험 결과

| 버전 | 관측과 시작 조건 | 성공률 | 해석 |
|---|---|---:|---|
| v1 | 고정 `front` 카메라, 기존 씬 | 50% | 선생 성공률을 재현했지만 씬에 강하게 종속 |
| v1 분포 이동 | 같은 정책, 새 바닥·조명 | 15% | 외부 카메라 기반 정책의 배치 민감성 확인 |
| v2 | 손목 카메라, 홈 자세 시작 | 5% | 첫 관측에 블록이 없으면 특권 지식이 학생에게 전달되지 않음 |
| v2.1 | 손목 카메라, 블록 조준 후 시작 | 25% | 가시성 확보만으로 성공률 5배 증가 |
| v2.2 | v2.1 조건, 성공 에피소드 200개 | 30% | 데이터 4배 증가는 5%p 개선에 그침 |

이 실험에서 얻은 가장 중요한 교훈은 다음과 같습니다.

- 학습 loss가 낮아도 관측에 없는 정보는 배울 수 없습니다.
- 데이터 양보다 태스크에 필요한 정보가 적절한 순간에 보이는지가 더 큰 지렛대입니다.
- 선생 궤적에 정확한 놓기와 실패 복구가 없으면 학생도 그 행동을 모방하기 어렵습니다. 이는 학생이 선생 성공률을 절대 넘을 수 없다는 수학적 상한이 아니라, 데이터가 제공하는 행동 범위와 품질이 실용적인 성능 한계를 만든다는 뜻입니다.
- 현재 남은 병목은 파지 순간의 가림, 운반 중 목표 구역의 시야 이탈, 단일 프레임 관측의 움직임 정보 부족입니다.

### DAgger 교정 루프

DAgger(Dataset Aggregation)는 선생의 성공 궤적만 한 번 학습하고 끝내지 않고, **학생이 실제로 방문한 실패 상태에 대한 선생의 교정 행동을 다시 데이터에 추가하는 반복 모방학습 방법**입니다.

```text
초기 선생 데이터로 학생 학습
  → 학생 정책을 직접 실행
  → 학생이 빗나가거나 멈춘 상태 수집
  → 그 상태에서 선생이 해야 할 행동을 라벨링
  → 기존 데이터에 교정 데이터 추가
  → 재학습 및 재평가
```

일반 행동복제는 선생이 정상적으로 지나간 상태만 보기 때문에, 배포 중 작은 오차로 낯선 상태에 들어가면 복구하지 못하는 분포 이동 문제가 생깁니다. DAgger는 학생 자신의 실패 상태를 다음 학습 데이터로 사용해 이 문제를 줄입니다. 이 프로젝트에서는 ACT가 블록을 밀거나 파지를 놓치거나 목표 구역을 잃은 순간을 System 2가 교정하는 방식으로 적용할 계획입니다.

## 구현된 제어 흐름

현재 참고 구현의 System 2 도구 흐름은 다음과 같습니다.

```text
자연어 명령
  → observe / look
  → find_and_aim(target)
  → move_forward / move_view / set_joints
  → grab_ahead / place_down
  → 시각 재확인
  → done

반복 작업인 경우
  → delegate_to_policy
  → ACT가 카메라+관절 상태만으로 25Hz 실행
  → 실패 시 System 2가 다시 관찰하고 복구
```

구현된 주요 구성 요소는 다음과 같습니다.

- MuJoCo 기반 SO-101 Gymnasium 환경
- DLS(Damped Least Squares) IK와 위치·방향 제어
- 클로머신 방식의 삽입형 `pick` 레시피와 `place` 레시피
- 손목 카메라 색 검출 및 적응형 센터링
- Claude tool-use 기반 System 2 오케스트레이터
- 자율 데이터 생성과 LeRobotDataset 저장
- ACT v1/v2/v2.1/v2.2 평가 및 영상 기록
- 로컬 VLM 숨바꼭질 실험
- 미지의 몸 자기 발견 실험
- Whisper STT와 TTS를 연결한 음성·채팅 콕핏

시뮬레이션 참고 구현의 핵심 코드는 이 저장소의 [`kwon_lab/`](./kwon_lab)에 포함되어 있습니다. 원본은 [`kihyeonkwon/lerobot-kwonlab`](https://github.com/kihyeonkwon/lerobot-kwonlab)이며, 검증 당시에는 upstream LeRobot 커밋 `a9879e69` 위에 `kwon_lab/`을 얹어 실행했습니다. 현재 저장소의 LeRobot 0.6.0 환경에서는 아래 Stage 0 범위까지 호환성을 확인했습니다. 당시 없었던 현행 v2.2 체크포인트와 핵심 데이터셋은 이후 인계받아 현재 `artifacts/`에 Git LFS로 포함했습니다.

### 통합 실행 검증 — Stage 0

2026-08-11에 Windows, Python 3.12, LeRobot 0.6.0, MuJoCo 3.11 환경에서 다음 항목을 검증했습니다.

- 한글이 포함된 Windows 경로에서도 MJCF와 상대 자산을 메모리 폴백으로 로드
- MuJoCo 환경 리셋, 블록 랜덤화, 640×480 손목캠 렌더링, 관절 상태 관측
- DLS IK 이동: 테스트 목표에서 최종 위치 오차 1.49mm
- 동일 시드의 `pick()` 성공 및 `place()` 물리 실행
- 자율 데이터 생성기 전체 실행: 5회 시도 중 성공 에피소드 1개, 193프레임, 25FPS
- LeRobotDataset 0.6.0 형식의 Parquet·MP4·메타데이터 저장
- ACT 0.6.0 정책·전처리 로더 API 호환성 확인
- System 2의 ACT 로더 호출 인자 오류 수정 및 체크포인트 누락 오류 메시지 추가

Stage 0 당시에는 정책 체크포인트가 없어 환경·IK·레시피·데이터 생성 경로까지만 검증했습니다. 이후 Mac 산출물 브랜치를 인계받아 ACT v2.2의 `pretrained_model`과 전처리·정규화 설정을 확보했고, 현재 검토 브랜치의 `artifacts/checkpoints/act_pick_v22/pretrained_model/`에 Git LFS로 포함합니다. 따라서 `git lfs pull` 후 실제 가중치 로드와 롤아웃을 재현할 수 있습니다. v1·v2.1과 옵티마이저를 포함한 전체 학습 체크포인트는 현행 실행에 필요하지 않아 포함하지 않습니다.

현재 성공 판정은 블록 중심 거리 대신 **블록 바닥 면적의 75% 이상이 반지름 5cm의 목표 구역 안에 포함되고, 블록 중심 높이가 5cm 미만인 상태**입니다. 블록의 현재 회전까지 반영한 면적 포함률을 계산하며, 환경 보상·데이터 생성·ACT 평가·System 2 외부 채점이 모두 같은 판정을 사용합니다.

### 놓기 높이 벤치마크

2026-08-11에 동일한 20개 시드로 엔드이펙터 놓기 높이 8·6·5·4cm를 각각 평가했습니다. 픽셀 렌더링만 생략하고 물리·IK·그랩·놓기·1초 안착 판정은 동일하게 유지했습니다.

| 엔드이펙터 높이 | 실제 블록 바닥 평균 높이 | 안정 성공률 | 평균 포함률 | 성공 시 평균 중심 오차 | 평균 IK 오차 |
|---:|---:|---:|---:|---:|---:|
| 8cm | 4.20cm | 80% | 86.9% | 1.61cm | 1.59mm |
| 6cm | 2.22cm | **85%** | **88.1%** | 1.77cm | 1.75mm |
| 5cm | 1.34cm | 80% | 86.0% | 2.28cm | 1.69mm |
| 4cm | 0.62cm | 80% | 85.1% | 2.55cm | 1.59mm |

종합 추천은 **6cm**입니다. 8cm가 성공한 경우의 중심 오차는 0.16cm 더 작았지만, 6cm가 안정 성공률과 전체 평균 포함률에서 가장 좋았습니다. 4~5cm는 테이블과 가까워질수록 블록을 누르거나 밀 가능성이 커지며 중심 오차가 증가했습니다. 작업자 승인에 따라 기본 `place()` 엔드이펙터 높이를 12cm에서 **6cm로 변경했습니다**.

6cm 적용과 함께 놓기 궤적을 `목표 위 안전 높이 이동 → 수직 하강 → 안정화 → 블록-엔드이펙터 오프셋 보정 → 릴리스 → 안착 대기 → 수직 이탈` 순서로 개선했습니다. 50개 고정 시드 검증에서 집기 성공률 94%, 전체 안정 성공률 86%를 기록했고, 성공한 43건의 목표 포함률은 모두 100%, 평균 중심 오차는 0.75cm였습니다. `place()`가 반환한 성공 여부와 추가 1초 안정화 후 판정의 불일치는 0건이었습니다.

성공 판정 경계도 별도로 확인했습니다. 테이블 위 중앙 배치는 성공, 중앙에 있지만 공중에 든 블록은 실패, 4cm 중심 오프셋에서는 블록 회전에 따라 포함률 71.7%는 실패·75.28%는 성공으로 판정되어 75% 면적 기준과 높이 조건이 함께 적용됩니다.

## 그랩 개발에서 얻은 교훈

초기 SO-101 모델은 시각적으로 손가락 사이가 비어 있어도 MuJoCo 충돌 계산에서는 메시의 볼록 껍질이 내부 공간을 막고 있어 어떤 궤적으로도 물체를 잡을 수 없었습니다. 텔레포트 기반 결정 실험이 0/45를 기록한 뒤, 그랩용 충돌 프리미티브가 적용된 Menagerie 모델로 교체해 37/45를 확인했습니다.

최종 그랩 레시피는 위에서 수직으로 내려찍는 대신, 45도 자세에서 벌린 그리퍼를 손가락 축 방향으로 약 6cm 삽입한 뒤 닫고 들어 올리는 방식입니다.

전이 가능한 디버깅 원칙은 다음과 같습니다.

1. 위치 IK, 방향 IK, 접촉 기하, 충돌 모델 순서로 한 층씩 확인합니다.
2. 손 튜닝이 반복해서 실패하면 자동 탐색으로 전환합니다.
3. 넓은 자동 탐색이 전멸하면 파라미터가 아니라 전제와 모델을 검증합니다.
4. 시각 형상과 물리 충돌 형상은 별도로 확인합니다.
5. 성공한 레시피는 즉시 함수로 봉인하고 성공률을 기록합니다.

## 로드맵

```text
S0 환경 검증 ✅
  → S2 SO-101 시뮬레이션 씬 ✅
  → S3 자연어→System 2→행동 ✅
  → S4 자율 데이터 생성 MVP ✅
  → S5 ACT 증류·평가 ✅
  → S6.5 미지의 몸 v0 ✅
  → S6 스킬 라이브러리 ⬜
  → S7 실물 이식 ⬜
```

### 다음 기술 우선순위

1. 놓기 높이와 궤적을 개선하여 선생 성공률을 먼저 높입니다.
2. 운반 중 `green_zone`을 다시 조준하여 놓기 국면의 가시성을 확보합니다.
3. `n_obs_steps=2` 이상을 적용하여 움직임과 가림 전후 정보를 제공합니다.
4. 같은 미학습 시드로 재평가하여 각 변경의 효과를 분리합니다.
5. System 1 실패 지점에서 System 2가 교정 데이터를 만드는 DAgger 루프를 수행합니다.
6. 전제조건·효과·검증기·타임아웃·복구 전략을 가진 스킬 레지스트리를 구현합니다.
7. 퍼팅→투척→움직이는 공 막기·잡기 순으로 새 스킬 획득 엔진을 확장합니다.
8. `unknown body --oneeye`, `--lift`, 모터 순서 셔플로 embodiment 독립성을 검증합니다.
9. 시뮬레이션 검증 후 실물 SO-101에서 sim-to-real 격차를 측정합니다.

파라미터 몇 개로 해결 가능한 스킬에는 ACT를 사용하지 않고 CEM이나 고전 제어기를 사용합니다. ACT는 시각 피드백, 시간적 반응, 복구처럼 폐루프 정책이 필요한 문제에 집중합니다.

## 프로젝트 운영 규칙

- 실험은 한 번에 변인 하나만 변경합니다.
- 본 실험 전에 가능한 경우 CV 심판이나 특권 채점을 이용한 드라이런으로 제어 루프를 검증합니다.
- 특권 물리는 외부 채점과 데이터 라벨링에만 사용합니다.
- API 키와 토큰은 `.env` 등 로컬 환경에서 관리하고 Git에 커밋하지 않습니다.
- 대용량 데이터셋, 영상 원본, 모델 체크포인트는 GitHub 일반 Git에 올리지 않습니다.
- 작업 완료 시 TODO, 완료 기록, 중요한 결정과 정량 결과를 문서에 함께 갱신합니다.
- 커밋되지 않은 변경을 다른 브랜치나 스냅샷 작업으로 덮어쓰지 않습니다.
- 실행 과정에서는 각 단계에서 무엇을 왜 하는지 설명하고 결과를 수치로 남깁니다.
- 시뮬레이션 우선 원칙을 유지하며, 실물 구매와 변경은 명확한 검증 필요가 생긴 뒤 진행합니다.

## 실물 렌즈 뚜껑 프로젝트 기준선

아래 내용은 SO-101 리더-팔로워 로봇을 사용해 **렌즈 뚜껑을 열린 상자 안에 넣는 작업**을 사람 시연 데이터로 학습하고, ACT 정책으로 팔로워암을 자율 실행한 실물 프로젝트 기록입니다. Dual-Brain의 자율 데이터 생성·실패 복구·sim-to-real 결과와 비교할 기준선으로 유지합니다.

### 현재 상태

- SO101 리더암·팔로워암 조립, USB 포트 식별, 캘리브레이션 완료
- Innomaker U20CAM-720P 카메라 연결 및 학습 장면 고정 완료
- 초기 시연 데이터셋 `version1` 수집·검수 완료
- ACT 모델 `act_lens_cap_v1` 학습 완료
- 팔로워암 단독 자율 실행(rollout) 확인 완료
- 실행 환경을 Windows에서 macOS로 이전
  - follower 포트: `/dev/tty.usbmodem5B140307781`
  - leader 포트: `/dev/tty.usbmodem5B3D0456881`
  - 전방 카메라: OpenCV index `0`
  - Windows에서 사용한 follower 캘리브레이션 파일 이전 완료
- macOS 고정 조건 정식 rollout 평가 10회 완료
  - 최종 성공: 2회 (2회차, 7회차)
  - 최종 실패: 8회
  - 성공률: 20%
  - 모든 회차에서 뚜껑 접근은 수행함
  - 뚜껑 집기 성공: 4회
  - 집기에 성공한 4회 중 상자 투입 성공: 2회
- 집기 실패를 보강하기 위한 성공 시연 데이터셋 `version2` 수집 완료
  - 로컬 데이터셋: `DY-01/lens_cap_into_box_version2`
  - 50개 에피소드, 45,000 프레임, 30 FPS
  - 뚜껑 위치를 작업 영역 안에서 무작위로 바꿔 수집
  - 텔레오퍼레이션 수집 시 `robot.max_relative_target=10` 사용
- `version1`과 `version2`를 통합한 학습 데이터셋 `v1+v2` 생성 완료
  - 업로드용 ID: `DY-01/lens_cap_into_box_v1_v2`
  - 100개 에피소드, 90,000 프레임, 30 FPS
  - Hugging Face 업로드 완료
- ACT 모델 `act_lens_cap_v2` 학습 완료
  - Google Colab Tesla T4에서 40,000 step 학습
  - 최종 checkpoint 저장 완료
- `act_lens_cap_v2` 고정 조건 정식 rollout 평가 10회 완료
  - 최종 성공: 2회
  - 최종 실패: 8회
  - 성공률: 20% (v1과 동일)
  - 평가 데이터: `rollout_act_lens_cap_v2_mac`, 8,529 프레임

현재 v1과 v2 모두 고정 조건 정식 평가에서 20% 성공률을 기록했습니다. 데이터 수를 100개로 늘린 것만으로는 집기 병목이 해소되지 않았으므로, 다음 단계의 목적은 **v2 실패 영상을 분석해 보강할 동작 품질과 조건을 좁히는 것**입니다.

## 하드웨어 구성

| 구성품 | 역할 | 현재 macOS 설정 |
|---|---|---|
| SO101 follower | 자율 실행 팔 | `/dev/tty.usbmodem5B140307781`, 별도 12V 전원 |
| SO101 leader | 시연 데이터 수집용 팔 | `/dev/tty.usbmodem5B3D0456881` (자율 실행에는 불필요) |
| Feetech STS3215-12V | 팔로워 관절 서보 6개 | ID 1~6 |
| Innomaker U20CAM-720P | 전방 카메라 | OpenCV index `0`, 640x480, 30 FPS |

이전 Windows 설정은 follower `COM3`, leader `COM5`, 전방 카메라 index `1`이었습니다.

팔로워 관절 ID는 다음과 같습니다.

| ID | 관절 |
|---:|---|
| 1 | `shoulder_pan` |
| 2 | `shoulder_lift` |
| 3 | `elbow_flex` |
| 4 | `wrist_flex` |
| 5 | `wrist_roll` |
| 6 | `gripper` |

### 연결 원칙

- 포트 이름과 OpenCV 카메라 index는 OS와 물리적 연결 순서에 따라 바뀔 수 있으므로, 연결 구성을 바꾼 뒤에는 반드시 다시 확인합니다.
- 현재 MacBook에는 USB-A 포트가 없어 카메라를 USB-C 허브에 연결합니다. 프레임 시간초과가 발생하면 다른 장치를 분리하거나, 로봇과 카메라를 서로 다른 USB-C 포트/허브로 나누거나, 전원형 허브를 사용합니다.
- 자율 실행 시에는 팔로워와 카메라만 연결합니다. 리더암은 필요하지 않습니다.
- 관절을 손으로 만지거나 케이블을 만질 때는 팔로워 12V 전원을 먼저 분리합니다.

## 개발 환경

- 현재 실행 OS: macOS (Apple Silicon)
- 이전 실행 OS: Windows
- Python: 3.12
- 패키지 관리: `uv`
- LeRobot: `0.6.0`
- 로컬 자율 실행 장치: CPU
- 학습 환경: Google Colab Tesla T4 GPU

프로젝트 의존성은 다음 파일에서 관리합니다.

- `pyproject.toml`
- `uv.lock`
- `.python-version`

프로젝트 명령은 항상 이 폴더에서 실행합니다.

```bash
cd /Users/doyounglim/Desktop/SO101
```

## 캘리브레이션

리더와 팔로워는 각각 별도로 캘리브레이션합니다. 기존 캘리브레이션이 있으면, 재실행 시 표시되는 질문에서 `Enter`를 눌러 기존 파일을 사용합니다. `c`를 입력하면 새 캘리브레이션이 시작됩니다.

현재 follower는 Windows에서 실제 학습·실행에 사용한 캘리브레이션 파일을 다음 macOS 경로로 이전해 사용합니다.

```text
/Users/doyounglim/.cache/huggingface/lerobot/calibration/robots/so_follower/so101_follower.json
```

리더는 macOS에서 포트를 확인하고 2026-07-31에 새로 캘리브레이션했습니다.

```text
leader port:
/dev/tty.usbmodem5B3D0456881

leader calibration:
/Users/doyounglim/.cache/huggingface/lerobot/calibration/teleoperators/so_leader/so101_leader.json
```

실행 중 기존 캘리브레이션을 사용할지 묻는 경우 `Enter`를 누릅니다. `c`를 누르면 해당 파일을 새 캘리브레이션으로 덮어쓰므로, 모터 교체나 조립 변경처럼 재캘리브레이션이 필요한 경우가 아니면 누르지 않습니다.

팔로워의 3번 서보(`elbow_flex`)는 내부 기어 파손으로 교체했으며, 교체 후 ID 3 설정과 팔로워 재캘리브레이션을 완료했습니다.

## 카메라·포트 확인

```bash
# 카메라 index 확인
uv run lerobot-find-cameras opencv

# 특정 모터 버스의 포트 찾기
uv run lerobot-find-port
```

현재 macOS 작업 카메라는 `Camera #0`입니다. 카메라를 다른 USB 포트로 옮겼다면 결과를 확인한 뒤 모든 명령의 `index_or_path` 값을 바꿔야 합니다.

## 시연 데이터셋

### 작업 정의

```text
Place the lens cap into the open box.
```

### 최종 학습 데이터

| 항목 | 값 |
|---|---|
| 데이터셋 이름 | `version1` |
| 로컬 경로 | `lerobot_data/lens_cap_into_box_version1` |
| 에피소드 수 | 50 |
| 총 프레임 | 45,000 |
| 에피소드 길이 | 30초 |
| 목표 FPS | 30 |
| 카메라 | 전방 카메라 1대, 640x480 |

데이터셋에는 영상뿐 아니라 다음 정보가 같이 저장됩니다.

- `observation.images.front`: 카메라 영상
- `observation.state`: 팔로워의 실제 6개 관절 상태
- `action`: 리더암 시연에서 나온 6개 관절 명령
- `timestamp`, `frame_index`, `episode_index`, `task_index`

따라서 ACT는 영상만으로 학습하는 것이 아니라, **카메라 영상 + 현재 관절 상태를 보고 다음 관절 동작을 예측**합니다.

### 집기 보강 데이터

| 항목 | 값 |
|---|---|
| 데이터셋 이름 | `version2` |
| 로컬 경로 | `lerobot_data/lens_cap_into_box_version2` |
| 에피소드 수 | 50 |
| 총 프레임 | 45,000 |
| 에피소드 길이 | 30초 |
| 목표 FPS | 30 |
| 뚜껑 배치 | 표시된 작업 영역 내 무작위 위치 |

`version1`과 같은 카메라 해상도, 관절 상태, 행동 형식으로 저장됐습니다.

### 통합 학습 데이터

| 항목 | 값 |
|---|---|
| 데이터셋 이름 | `v1+v2` |
| 업로드용 ID | `DY-01/lens_cap_into_box_v1_v2` |
| Hugging Face | `https://huggingface.co/datasets/DY-01/lens_cap_into_box_v1_v2` |
| LeRobot 데이터셋 태그 | `v3.0` |
| 로컬 경로 | `lerobot_data/lens_cap_into_box_v1_v2` |
| 에피소드 수 | 100 |
| 총 프레임 | 90,000 |
| FPS | 30 |

Hugging Face 리포지토리 이름에는 `+`를 쓸 수 없으므로, 업로드할 때는 `v1_v2`를 사용합니다.

### 시연 수집 예시

```powershell
uv run lerobot-record --robot.type=so101_follower --robot.port=COM3 --robot.id=so101_follower --robot.cameras="{ front: {type: opencv, index_or_path: 1, width: 640, height: 480, fps: 30}}" --teleop.type=so101_leader --teleop.port=COM5 --teleop.id=so101_leader --dataset.repo_id=DY-01/<dataset_name> --dataset.single_task="Place the lens cap into the open box." --dataset.num_episodes=10 --dataset.episode_time_s=30 --dataset.reset_time_s=10 --dataset.push_to_hub=false --display_data=true
```

시연 영상은 성공한 동작만 보관합니다. 손이 화면에 오래 남거나, 집기·놓기가 실패했거나, 기계적 이상이 발생한 에피소드는 학습 데이터에서 제외합니다.

## ACT 학습

ACT(Action Chunking Transformer)는 카메라 영상과 현재 관절 상태를 입력으로 받고, 이후 관절 명령 묶음(action chunk)을 예측하는 정책입니다.

Google Colab T4 GPU에서 학습했습니다.

```bash
pip install -q "lerobot[training]==0.6.0"

lerobot-train \
  --dataset.repo_id=DY-01/lens_cap_into_box_version1 \
  --dataset.video_backend=pyav \
  --policy.type=act \
  --policy.device=cuda \
  --policy.push_to_hub=false \
  --output_dir=/content/drive/MyDrive/SO101/act_lens_cap_v1 \
  --job_name=act_lens_cap_v1 \
  --batch_size=8 \
  --num_workers=2 \
  --steps=5000 \
  --save_freq=1000 \
  --log_freq=50
```

초기 5,000 step 학습 후 동일 모델을 재개하여 총 20,000 step까지 학습했습니다.

| 항목 | 값 |
|---|---|
| 정책 | ACT |
| 총 학습 step | 20,000 |
| 최종 training loss | 약 0.16 |
| 모델 이름 | `act_lens_cap_v1` |

학습 loss는 시연 데이터를 얼마나 잘 재현하는지의 지표일 뿐, 실제 로봇 성공률과 같지는 않습니다. 실제 성공률은 rollout 평가로 측정합니다.

## 모델 보관

학습된 모델은 로컬에서 다음 폴더에 둡니다.

```text
models/act_lens_cap_v1/
```

이 폴더는 `model.safetensors` 등 대용량 가중치를 포함하므로 GitHub 일반 Git에는 올리지 않습니다. 원본은 Google Drive 또는 Hugging Face에 별도로 보관합니다.

## 자율 실행

자율 실행에는 팔로워와 카메라만 사용합니다. 실행 전에 카메라 구도, 상자·뚜껑 위치, 팔의 시작 자세를 학습 데이터 수집 시점과 최대한 일치시킵니다.

### 홈 자세 복귀 원칙

앞으로 작성하거나 수정하는 모든 자율 실행·평가 코드는 rollout이 성공, 실패, 시간 제한 종료 중 어느 경우로 끝나더라도 **정책을 먼저 정지한 뒤 팔로워를 안전한 속도로 홈 자세로 복귀**시켜야 합니다. 홈 자세는 캘리브레이션의 0점이 아니라, 이 작업을 시작할 때 사용하는 사용자 지정 관절 자세입니다.

홈 복귀는 ACT 정책이 학습해서 수행할 동작이 아니라 별도 제어 로직으로 처리합니다. 통신 오류, 충돌 위험, 비정상 진동 또는 비상 정지가 감지된 경우에는 홈 복귀를 시도하지 않고 즉시 모터를 정지하거나 12V 전원을 분리합니다. 홈 관절값은 실제 로봇에서 충돌 없이 확인한 뒤 코드 설정값으로 관리합니다.

```bash
uv run lerobot-rollout --strategy.type=base --policy.path=/Users/doyounglim/Desktop/SO101/models/act_lens_cap_v1 --device=cpu --robot.type=so101_follower --robot.port=/dev/tty.usbmodem5B140307781 --robot.id=so101_follower --robot.max_relative_target=5 --robot.cameras="{ front: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" --task="Place the lens cap into the open box." --duration=30 --display_data=false
```

`--robot.max_relative_target=5`는 한 번에 너무 큰 관절 명령이 들어올 때 제한하는 안전장치입니다. 경고가 나오더라도 값을 높이거나 제거하지 않습니다.

## 자율 실행 평가

LeRobot 0.6.0에서는 `lerobot-record`가 리더암 시연 수집용입니다. 학습 정책 평가와 자동 영상 기록에는 `lerobot-rollout --strategy.type=episodic`을 사용합니다.

평가 데이터셋 이름은 반드시 `rollout_`으로 시작해야 합니다.

```bash
uv run lerobot-rollout --strategy.type=episodic --policy.path=/Users/doyounglim/Desktop/SO101/models/act_lens_cap_v1 --device=cpu --robot.type=so101_follower --robot.port=/dev/tty.usbmodem5B140307781 --robot.id=so101_follower --robot.max_relative_target=5 --robot.cameras="{ front: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}}" --task="Place the lens cap into the open box." --dataset.repo_id=DY-01/rollout_act_lens_cap_v1_mac --dataset.root=/Users/doyounglim/Desktop/SO101/lerobot_data/rollout_act_lens_cap_v1_mac --dataset.single_task="Place the lens cap into the open box." --dataset.fps=30 --dataset.num_episodes=10 --dataset.episode_time_s=30 --dataset.reset_time_s=10 --dataset.video=true --dataset.push_to_hub=false --dataset.streaming_encoding=false --dataset.encoder_threads=1 --strategy.reset_to_initial_position=true --display_data=false
```

### 평가 진행 방식

1. 각 회차가 끝나면 정책을 정지하고, 별도 홈 복귀 제어로 팔을 안전한 시작 자세에 되돌린 뒤 상자와 뚜껑을 시작 위치에 되돌립니다.
2. 영상과 관절 로그가 별도 rollout 데이터셋에 저장됩니다.
3. 영상 검수 후 성공 여부를 표시합니다.
4. 성공률은 `성공 에피소드 수 / 완전한 평가 에피소드 수`로 계산합니다.

현재 CPU 추론 속도는 약 3~5Hz 경고가 발생할 수 있습니다. USB-C 허브 연결 카메라에서 프레임 시간초과가 발생할 수 있으므로 다른 USB 장치를 최소화하고, 평가 중에는 `--display_data=false`, `--dataset.streaming_encoding=false`를 사용합니다.

### ACT v1 정식 평가 결과

2026-07-31에 macOS의 고정된 카메라·상자·뚜껑 위치에서 10개 에피소드를 평가했습니다.

| 회차 | 뚜껑 접근 | 뚜껑 집기 | 상자 접근 | 상자 투입 | 최종 결과 |
|---:|:---:|:---:|:---:|:---:|:---:|
| 1 | O | X | - | - | 실패 |
| 2 | O | O | O | O | 성공 |
| 3 | O | O | O | X | 실패 |
| 4 | O | O | O | X | 실패 |
| 5 | O | X | - | - | 실패 |
| 6 | O | X | - | - | 실패 |
| 7 | O | O | O | O | 성공 |
| 8 | O | X | - | - | 실패 |
| 9 | O | X | - | - | 실패 |
| 10 | O | X | - | - | 실패 |

- 전체 성공률: `2 / 10 = 20%`
- 뚜껑 접근 성공률: `10 / 10 = 100%`
- 뚜껑 집기 성공률: `4 / 10 = 40%`
- 집기 성공 후 상자 투입 성공률: `2 / 4 = 50%`
- 평가 데이터: `lerobot_data/rollout_act_lens_cap_v1_mac`
- 저장 결과: 10개 에피소드, 8,567 프레임

가장 큰 병목은 뚜껑 집기 단계입니다. 먼저 실패 영상에서 그리퍼의 접근 위치, 높이, 닫는 시점을 비교하고, 해당 실패 조건을 중심으로 보강 데이터를 수집합니다.

### ACT v2 정식 평가 결과

2026-07-31에 v1과 같은 고정된 카메라·상자·뚜껑 위치에서 10개 에피소드를 평가했습니다.

- 전체 성공률: `2 / 10 = 20%`
- 평가 데이터: `lerobot_data/rollout_act_lens_cap_v2_mac`
- 저장 결과: 10개 에피소드, 8,529 프레임

v2는 100개 성공 시연으로 학습했지만 v1의 20%를 넘지 못했습니다. 따라서 다음 보강에서는 무작위 위치를 더 넓히기 전에, 성공·실패 영상에서 그리퍼 중심 위치, 접근 높이, 닫는 시점, 파지 후 들어 올림을 비교해 실패 원인을 먼저 특정합니다.

### ACT v2 홈 자세 평가 결과

2026-08-04에 실제 태스크 시작 자세를 관절값으로 저장하고, 각 에피소드 종료 후 별도 제어로 홈 자세에 복귀하는 조건에서 v2를 10회 평가했습니다.

- 전체 성공률: `4 / 10 = 40%`
- 평가 데이터: `lerobot_data/rollout_act_lens_cap_v2_home_mac`
- 홈 자세 설정: `config/lens_cap_home_pose.json`

홈 자세를 고정한 뒤 성공률은 기존 20%에서 40%로 상승했습니다. 다만 표본이 10회이므로, 이 결과만으로 데이터 보강 효과와 홈 자세 효과를 분리해 확정하지는 않습니다.

관찰된 실패 원인:

1. 뚜껑의 위치를 정확히 중심에 맞춰 집기보다, 보강 데이터에서 자주 나타난 접근 위치 중 하나로 향하는 양상이 보입니다. 넓은 무작위 위치를 50개 시연으로만 수집하면 위치별 예시가 부족해질 수 있습니다.
2. 그리퍼가 뚜껑을 밀어 위치가 바뀐 뒤에도 새 카메라 관측에 맞춰 재탐색·재접근하지 못하고, 원래 접근 동작을 계속하는 양상이 보입니다. 기존 데이터에는 이런 복구 상태에서 최종 성공으로 이어지는 시연이 없습니다.

### 평가 중 키 조작

- `→`: 현재 에피소드를 즉시 끝내고 저장
- `←`: 현재 에피소드를 버리고 다시 기록
- `Esc`: 평가 세션 종료

`→`로 조기 종료한 에피소드는 부분 기록이므로 성공률 평가에서 제외하거나 삭제합니다.

부분 에피소드 삭제 예시:

```powershell
uv run lerobot-edit-dataset --repo_id=DY-01/rollout_act_lens_cap_v1 --root="<평가 데이터 폴더>" --new_repo_id=DY-01/rollout_act_lens_cap_v1 --new_root="<평가 데이터 폴더>" --operation.type=delete_episodes --operation.episode_indices="[9]" --push_to_hub=false
```

## 알려진 이슈와 대응

| 증상 | 원인 또는 상태 | 대응 |
|---|---|---|
| `OpenCVCamera(0) read failed` 또는 frame timeout | 허브 대역폭·전력 부족, 카메라 점유 또는 프레임 갱신 실패 | 카메라 앱을 종료하고 USB를 재연결합니다. 다른 USB 장치를 줄이거나 로봇과 카메라를 다른 포트/전원형 허브로 분리합니다. |
| `Record loop is running slower` | 로컬 CPU 추론이 목표 30Hz보다 느림 | 성능 평가에 기록하고, 향후 CUDA 가능 PC 또는 더 가벼운 실행 환경 검토 |
| `Relative goal position ... clamped` | 목표 관절 위치와 현재 위치 차이가 안전 제한보다 큼 | rollout에서는 `max_relative_target=5`를 유지합니다. 텔레오퍼레이션 수집에서는 리더·팔로워 시작 자세를 맞춘 뒤 `10`을 사용했습니다. |
| `Could not write TorqueEnable on id=5` | wrist_roll 모터 통신/토크 설정이 일시 실패 | 12V·USB를 재연결하고 기존 캘리브레이션 파일로 연결 점검 |
| 서보 기어 파손 | 팔로워 3번 모터의 내부 기어 손상 | STS3215-12V 교체, ID 3 설정 및 재캘리브레이션 완료 |

## Isaac Sim + Isaac Lab 향후 전환 후보

**현행 시뮬레이터는 MuJoCo입니다.** Apple Silicon에서 바로 실행할 수 있고, 현재 병목인 그랩·놓기 레시피와 에고센트릭 관측을 검증하는 데 충분하며, 이미 전체 데이터 생성·ACT 평가 파이프라인이 MuJoCo로 작동합니다.

Isaac Sim과 Isaac Lab은 당장의 다음 단계가 아니라 향후 전환 후보입니다. NVIDIA RTX 워크스테이션을 사용할 수 있고, 대규모 병렬 강화학습이나 고품질 시각 도메인 랜덤화가 실제 병목으로 확인될 때 전환을 검토합니다. 전환할 경우 Isaac Sim은 장면·물리·카메라 렌더링을, Isaac Lab은 병렬 환경의 데이터 생성과 정책 학습을 담당합니다.

현재 `simulation_assets/so101`에는 SO101의 MuJoCo(MJCF) 및 URDF 자산이 있습니다. Isaac 환경에는 이 로봇 자산을 USD/URDF 기반으로 가져오고, 상자·렌즈 뚜껑·책상·고정 RGB 카메라를 추가로 구성해야 합니다. 현재 자산은 그리퍼의 LeRobot 표현(`0=닫힘`, `100=열림`)과 동일한 제어 매핑이 아직 반영되지 않았으므로, 집기 학습 전에 이를 검증합니다.

### 시뮬레이션 데이터 생성 범위

1. 뚜껑을 화면 내 격자와 연속 좌표에 배치하고, 집기-운반-상자 투입까지 성공하는 궤적을 자동 생성합니다.
2. 뚜껑이 그리퍼에 밀려 좌·우·위·아래로 벗어난 상태를 의도적으로 만들고, 재탐색-재접근-재그립 후 성공하는 복구 궤적을 생성합니다.
3. 뚜껑·상자 위치, 크기, 마찰, 질량, 그리퍼 접촉, 책상 재질을 랜덤화합니다.
4. 조명, 그림자, 배경, 색상, 카메라 자세와 내부 파라미터, 이미지 노이즈를 랜덤화해 실제 웹캠과의 시각적 차이를 줄입니다.
5. 상태 기반 IK 또는 스크립트 전문가 정책으로 성공 궤적을 생성하고, 렌더링된 RGB 영상과 관절 행동을 LeRobot 호환 데이터셋 형태로 내보냅니다.

### 학습 및 실제 로봇 검증

1. 합성 데이터로 뚜껑 위치 인식과 복구 행동을 먼저 학습합니다.
2. 실제 로봇에서는 카메라 구도·물체 치수·그리퍼 접촉 차이를 보정하기 위한 소량의 고품질 시연만 수집합니다.
3. 합성 데이터와 실제 보정 데이터를 병합 또는 순차 미세 조정하고, 홈 자세 고정 조건에서 정식 10회 평가를 반복합니다.
4. 실제 실행에서는 `robot.max_relative_target=5`와 별도 홈 복귀 제어를 유지합니다.

### 실행 환경

현행 MuJoCo 개발과 ACT 학습은 Apple Silicon macOS 환경을 기준으로 합니다. 향후 Isaac Sim과 Isaac Lab으로 전환한다면 NVIDIA RTX GPU가 있는 Windows 또는 Linux 워크스테이션·서버가 필요합니다. 이 경우 macOS 장비는 실제 SO-101 연결·평가와 원격 시뮬레이터 확인에 사용합니다.

- [NVIDIA SO-101 sim-to-real 도메인 랜덤화 가이드](https://docs.nvidia.com/learning/physical-ai/sim-to-real-so-101/latest/09-strategy1-dr-teleop.html)
- [Isaac Lab 시각 도메인 랜덤화 가이드](https://docs.nvidia.com/learning/physical-ai/getting-started-with-isaac-lab/latest/transferring-robot-learning-policies-from-simulation-to-reality/03-bridging-the-gap-simulation-enhancement/01-visual-domain-randomization.html)
- [Isaac Sim 공식 요구사항](https://docs.isaacsim.omniverse.nvidia.com/latest/installation/requirements.html)

## 다음 단계

1. 손목 영상에서 블록이 그리퍼와 함께 움직이는지, 상단 영상에서 블록이 테이블에 멈췄는지를 결합해 운반 낙하 판정을 보강합니다.
2. 낙하 감시가 별도 검증에서 충분한 재현율과 낮은 오탐률을 확보한 뒤에만 `낙하 감지 → 재조준 → 재집기 → 운반 재개` 능동 복구에 연결합니다.
3. DAgger는 전체 복구 에피소드 대신 개입 직전 0.4~0.8초와 교사 복구 초반만 남긴 짧은 교정 구간으로 다시 만들고, 전체 프레임의 10~15%로 제한합니다.
4. 새 모델은 v2.2 정규화를 유지한 채 250·500스텝에서 같은 20개 시드로 평가하고, 기준선보다 좋아질 때만 50회 정식 평가합니다.
5. 실물 상단 U20CAM을 약 52cm 높이에 고정한 뒤 목표 마스크·작업영역·카메라 인덱스를 등록하고, 먼저 영상 판정만 실행하는 그림자 모드로 검증합니다.
6. 실물 SO-101에서는 저속·낮은 힘으로 시작해 집기 확인과 낙하 감시가 안정된 뒤 능동 재집기를 단계적으로 허용합니다.
7. MuJoCo에서 데이터 생성량이나 시각 도메인 랜덤화가 병목이 될 때 Isaac Sim·Isaac Lab 전환을 재검토합니다.

## GitHub 아티팩트 관리

재현에 필요한 학습 데이터와 ACT 체크포인트는 [`artifacts/`](./artifacts)에 선별해
Git LFS로 관리합니다. `*.safetensors`, `*.mp4`, `*.parquet` 등 대용량 파일은
`.gitattributes`의 LFS 규칙을 따르며, 설정 JSON과 메타데이터는 일반 Git에 저장합니다.

다음 항목은 계속 `.gitignore`로 제외합니다.

- `.venv/`와 Python 캐시
- 실행 중 계속 덮어쓰는 `outputs/`
- 정리되지 않은 로컬 `models/`, `lerobot_data/`, 임시 영상
- Hugging Face 토큰, API 키, `.env`, 기타 비밀 정보

업로드할 때는 `outputs/` 전체를 그대로 커밋하지 않고, 재현에 필요한 최종 데이터셋과
체크포인트만 `artifacts/`로 복사해 검수 후 커밋합니다. 이렇게 하면 코드 저장소의
구조를 유지하면서 GitHub clone/pull만으로 필요한 실험 자산도 받을 수 있습니다.
